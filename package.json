{
  "name": "frame-studio",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "worker:dev": "pnpm --dir workers/render-worker dev",
    "worker:build": "pnpm --dir workers/render-worker build",
    "worker:start": "pnpm --dir workers/render-worker start",
    "lint": "next lint && npx @typescript-eslint/cli lint packages/*/src/ types/*.ts"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
